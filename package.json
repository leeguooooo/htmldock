{
  "name": "htmldock",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "bin": {
    "htmldock": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "build:cli": "bun build ./src/cli.ts --compile --outfile dist/htmldock",
    "deploy": "wrangler-accounts --profile Xdreamstar2025 deploy",
    "dev": "wrangler dev"
  }
}
